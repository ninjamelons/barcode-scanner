namespace BScanner {
    namespace Types {
        class Transforms
        {
        public:
            Transforms();
            ~Transforms();
        private:
            double rotation;
            
        };
    }
}